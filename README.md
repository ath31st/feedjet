# 📺 Feedjet 📺

## Оглавление

1. [Описание проекта](#описание-проекта)
2. [Пример работы](#пример-работы)
3. [Технологии](#технологии)
6. [Архитектура проекта](#архитектура-проекта)
7. [Автор](#автор)
8. [Лицензия](#лицензия)

## Описание проекта

Проект Feedjet представляет собой систему организации показа на экранах-киосках RSS-лент, видео файлов, расписания мероприятий, прогноза погоды и других информационных ресурсов.

## Пример работы

Видео-демонстрация работы проекта:

[Пример работы]() будет добавлен позже

## Технологии

Список основных технологий и инструментов, использованных в проекте:

- Backend: TypeScript, Node.js, Express, Drizzle, Sqlite.
- Frontend: TypeScript, React, Vite, Radix UI, Tailwind CSS, Framer Motion, Zustand.
- Дополнительные: JWT, Nginx, tRPC, SSE.

## Архитектура проекта

Архитектура Feedjet построена вокруг принципов модульности и строгого разделения ответственностей. Клиент и сервер связаны типобезопасным контрактом через tRPC. Логика обработки данных (RSS, медиа, расписание) вынесена в отдельные слои и фоновые процессы.

### Клиент

Клиентская часть имеет в своей основе React с архитектурой Feature-Sliced Design (FSD). Такой подход позволяет изолировать бизнес-логику, хранить устойчивые контракты и развивать функциональные блоки независимо друг от друга.

Основные элементы клиентской архитектуры:

- App (routes/layouts/providers) — общие каркасы(слои) страницы, загрузка конфигурации, корневые провайдеры.

- Entities (entities) — модели домена (ленты, элементы фида, расписание), Zustand хранилища и tRPC-хуки.

- Pages (pages) — основные страницы приложения (главная(киоск), администраторская, логин, 401, 404).

- Widgets — сложные UI-композиции, собранные из нескольких фич и сущностей (например, ScheduleGrid, FeedCard).

- Features — бизнес-функции: управление лентами, расписанием, темами, настройками.

- Entities — модели домена (ленты, элементы фида, расписание), react-query хранилища и tRPC-хуки.

- Shared — универсальные компоненты, утилиты, константы и типы.

Клиент использует:

- React + Vite

- Tailwind CSS

- Framer Motion (анимации при смене виджетов, движение блоков RSS-ленты)

- Zustand (локальное состояние)

- @tanstack/react-query

- tRPC-клиент

Все данные приходят строго через tRPC-роуты. Мутации и запросы находятся в слоях Entities и Features, что минимизирует связанность.

---

### Сервер

Серверная часть проекта построена в стиле слоёной архитектуры:

- Config — конфигурация приложения.

- Errors — обработка ошибок.

- Integrations — интеграции с внешними сервисами (ADB, Fully Kiosk).

- Mappers — преобразование данных.

- Middleware —  промежуточный обработчик auth для tRPC запросов.

- Services (application layer) — бизнес-операции: обработка RSS, нормализация данных, обновление расписания, загрузка медиа.

- DB — хранилище (Drizzle ORM).

- Cron — фоновая обработка RSS, работа с кэшем изображений.

- SSE — Server-Sent Events.

- TRPC — контроллеры tRPC.

- Types — внутренние типы для серверной части.

- Utils — утилиты.

- Validations — валидация входных данных с помощью Zod.

Основные технологии:

- Node.js + Express

- Drizzle ORM

- SQLite 

- tRPC

- Sharp/FFmpeg (обработка медиа)

Работа с внешними источниками (RSS, изображения, видео) выполняется через изолированные адаптеры. Обработка тяжёлых задач вынесена в фоновый worker.

## Автор

- **Владимир Оленников**
- [GitHub](https://github.com/ath31st) | [LinkedIn](https://www.linkedin.com/in/ath31st/)

## Лицензия

Этот проект лицензируется в соответствии с лицензией MIT.
Подробнее в файле [LICENSE](LICENSE).
